{
    //Task to Compile using provided make file
    "tasks": [
        {
            "type": "shell",
            "label": "make",
            "command": "make",
            "options": {
                "cwd": "${fileWorkspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}